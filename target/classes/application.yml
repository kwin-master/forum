spring:
  profiles:
    active: dev

server:
  servlet:
    context-path: /forum

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.kwin.forum.entity
  configuration:
    map-underscore-to-camel-case: true